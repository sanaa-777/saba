<%- include('../partials/head') %>
<%- include('../partials/header') %>

<main class="main-content">
  <div class="container">
    <nav class="breadcrumbs">
      <a href="/">الرئيسية</a>
      <span class="separator">/</span>
      <span class="current">كاريكاتير</span>
    </nav>

    <section class="page-hero">
      <span class="eyebrow">رأي بصري</span>
      <h1 class="page-title">كاريكاتير</h1>
      <p>مساحة مخصصة للرسوم التعبيرية والقراءة البصرية السريعة ضمن عرض حديث ومتوازن.</p>
    </section>

    <div class="content-grid">
      <div class="content-area">
        <section class="section-block">
          <div class="section-header"><div><h2>أحدث الرسوم</h2><p>عرض مبسّط يبرز الفكرة البصرية ويزيد وضوح المحتوى.</p></div></div>
          <% if (typeof caricatures !== 'undefined' && caricatures.length > 0) { %>
            <div class="media-grid">
              <% caricatures.forEach(item => { %>
                <article class="media-card" onclick="openImageModal('<%= item.file_path %>', '<%= item.title %>')">
                  <div class="media-card-image">
                    <img src="<%= item.file_path %>" alt="<%= item.title %>" loading="lazy">
                  </div>
                  <div class="media-card-body">
                    <span class="eyebrow">كاريكاتير</span>
                    <h3><a href="javascript:void(0)"><%= item.title %></a></h3>
                    <% if (item.description) { %><p><%= item.description %></p><% } %>
                    <span class="card-date"><%= new Date(item.created_at).toLocaleDateString('ar-YE') %></span>
                  </div>
                </article>
              <% }); %>
            </div>
          <% } else { %>
            <div class="no-results"><p>لا توجد مواد كاريكاتير حالياً.</p></div>
          <% } %>
        </section>
      </div>
      <%- include('../partials/sidebar') %>
    </div>
  </div>
</main>

<%- include('../partials/footer') %>
